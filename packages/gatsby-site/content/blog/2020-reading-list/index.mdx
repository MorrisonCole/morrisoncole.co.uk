---
title: "2020 Reading List"
date: "2020-03-22"
updated: "2020-12-31"
description: "Books I read in 2020, ranked."
category: "Books"
image: "books.jpg"
imageAlt: "Stack of Books"
linkText: "Quarantine reading 📚"
draft: false
---

import {
  Avatar,
  List,
  ListItem,
  ListItemAvatar,
  ListItemIcon,
  ListItemText,
  Rating,
} from "@mui/material";
import { Star } from "@mui/icons-material";

<List dense>
  {props.data.books2020.edges[0].node.reviews
    .sort((a, b) => parseInt(b.rating) - parseInt(a.rating))
    .map(({ rating, book }) => (
      <ListItem
        button
        component="a"
        href={book.link}
        target="_blank"
        rel="noopener"
        key={book.id}
      >
        <ListItemAvatar>
          <Avatar
            style={{ height: "auto" }}
            variant="rounded"
            alt={book.title}
            src={book.image_url}
          />
        </ListItemAvatar>
        <ListItemText primary={book.title} secondary={book.authors[0].name} />
        <Rating name="read-only" value={rating} readOnly />
      </ListItem>
    ))}
</List>
