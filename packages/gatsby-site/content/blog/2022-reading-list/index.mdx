---
title: "2022 Reading List"
date: "2022-01-03"
updated: null
description: "Books I've read this year, ranked."
category: "Books"
image: "books.jpg"
imageAlt: "Stack of Books"
linkText: "What I've read so far 📚"
draft: false
slug: "2022-reading-list"
---

import {
  Avatar,
  List,
  ListItem,
  ListItemAvatar,
  ListItemIcon,
  ListItemText,
  Rating,
} from "@mui/material";
import { Star } from "@mui/icons-material";

<List dense>
  {props.data.books2022.edges[0].node.reviews
    .sort((a, b) => parseInt(b.rating) - parseInt(a.rating))
    .map(({ rating, book }) => (
      <ListItem
        button
        component="a"
        href={book.link}
        target="_blank"
        rel="noopener"
        key={book.id}
      >
        <ListItemAvatar>
          <Avatar
            style={{ height: "auto" }}
            variant="rounded"
            alt={book.title}
            src={book.image_url}
          />
        </ListItemAvatar>
        <ListItemText primary={book.title} secondary={book.authors[0].name} />
        <Rating name="read-only" value={rating} readOnly />
      </ListItem>
    ))}
</List>
