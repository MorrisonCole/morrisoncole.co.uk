import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  typography: {
    fontFamily: 'Roboto',
  },
  spacing: 4,
  palette: {
    primary: {
      main: '#007bff',
    },
  },
});

export default theme;
